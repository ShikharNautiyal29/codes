#include<bits/stdc++.h>
using namespace std;
int main()
{
    int max,i,p;
    char s[20];
    cin>>s;
    p=strlen(s);
    sort(s,s+p);
    //for(i=0;i<=strlen(s);i++)
    //{

    //}
    cout<<s;
}
